// Import Home component for redirection
import { Navigate } from "react-router-dom";
import Home from "./Home";

const Index = () => {
  // Simply render the Home component
  return <Home />;
};

export default Index;
